int foo(int x, int y)
{
	int val; 
	if (x > y) 
	{
		val = foo(x, (y + 1)); 
	}
	return val; 
}

int foobar(int result, int x, int y)
{
	int val; 
	if (x < result)
	{
		val = foobar(result, (x * 10), y); 

		if (x > y) 
		{
			x = (x / 10); 
			print x; 
		} else {
			y = (y * 10); 
			print y; 
		}
	}
	return 0; 
}

int main() 
{
	int x; 
	int y; 
	int result; 
	x = 11; 
	y = 10; 
	result = foo(x,y); 
	result = foobar(result, x, y); 
	return 0; 
}
