int foo(int x, int y)
{
	int result;
	result = 0;
	if (x > y)
	{
		result = foobar(x, y);
	}
	return result;
}

int foobar(int x, int y)
{
	int result;
	result = 0;
	if (y < x)
	{
		result = fu(x, y);
	}
	return result;
}

int fu(int x, int y)
{
	int result;
	result = 0;
	if (y % x == 1)
	{
		result = 0;
	} 
	else 
	{
		result = fubar(x, y);
	}
	return result;
}

int fubar(int x, int y)
{
	while !(y == x)
	{
		print y;
		y = y + 1;
	}
	
	return y;
}

int main() 
{
	int x;
	int y;
	x = 11;
	y = 0;
	print x;
	print y;
	foo(x,y);
}
