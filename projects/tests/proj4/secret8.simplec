int sum(int y, int x)
{
	int res; 
	while (x < y) 
	{
		res = (res + x); 
		x = (x + 1); 
	}
	return res; 
}

int main()
{
	int y; 
	int x; 
	int a; 
	int b; 
	y = 100; 
	x = 1; 
	a = sum(y, x); 
	print a; 
	b = sum(y, x); 
	print b; 
	return 0; 
	
}
